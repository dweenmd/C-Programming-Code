#include<stdio.h>
int main(){
    int a,b;
    printf("Enter two different numbers:");
    scanf("%d""%d",&a,&b);
    if (a>b){
        printf("Input number one is maximum than input number two");
    }
    else {
        printf("Input number two is maximum than input number one");
    }
    return 0;
}
