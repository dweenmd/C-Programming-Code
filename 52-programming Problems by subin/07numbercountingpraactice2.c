#include<stdio.h>
#include<string.h>
int main()
{
    int a;
    scanf("%d",&a);

    while (a--)
    {
        int count =0;
        char str[100000];
        printf("input\n");

        getchar();
        gets(str);
        puts(str);
        int n=strlen(str);
        printf("len: %d\n",n);
        for (int i=0;i<n;i++){

        }
    }

}
